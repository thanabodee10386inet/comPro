my_tuple = 1, 2, 3 # สร้าง tuple โดยไม่ต้องใช้วงเล็บ
print(my_tuple)  # แสดงผลลัพธ์จะเป็น (1, 2, 3)

a, b, c = my_tuple  # แยกค่า tuple ออกมาเป็นตัวแปร
print(a) # แสดงผลลัพธ์จะเป็น 1
print(b) # แสดงผลลัพธ์จะเป็น 2
print(c) # แสดงผลลัพธ์จะเป็น 3
